# PoC dependencies — runnable locally with `pip install -r requirements.txt`
#
# Streamlit is the only external dep for the demo UI.
# Everything else is stdlib (dataclasses, hashlib, json, pathlib, re).
# No OCR, no LLM client, no DB connection in the PoC.

streamlit>=1.30.0
pytest>=7.4.0
